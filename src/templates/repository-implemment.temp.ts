const repositoryImplTemp = `import 'package:dartz/dartz.dart';
import 'package:injectable/injectable.dart';

@Injectable(as: Repository)
class RepositoryImpl extends Repository {
  RepositoryImpl();

}

`;

export default repositoryImplTemp;
